# Copy this file to .env and add your actual API key
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Allow geists to execute code (Python, Bash, etc.)
# Default: true (safe because each geist runs in an isolated Docker container)
# Set to "false" to disable code execution
GEIST_ALLOW_EXECUTION=true
